pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Clone repository') {
            steps {
                git url: 'https://github.com/titi070/TP-Application-GPS.git', branch: 'main'
            }
        }

        stage('Install dependencies') {
            steps {
                bat 'C:\\Program Files\\Python312\\python.exe -m venv venv'
                bat 'venv\\Scripts\\activate && C:\\Python39\\python.exe -m pip install -r requirements.txt'
            }
        }

        stage('Run tests') {
            steps {
                bat 'venv\\Scripts\\activate && C:\\Python39\\python.exe -m unittest discover tests'
            }
        }

        stage('Deploy application') {
            steps {
                bat 'venv\\Scripts\\activate && C:\\Python39\\python.exe deploy.py'
            }
        }
    }
}
